using UnityEngine;

public class MountainGenerator : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        VerticesMovement();
    }

    private void VerticesMovement()
    {
        
    }
}
